backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)

# This line is required to use Netlify Identity
# without configuring a GitHub/GitLab/Bitbucket app
local_backend: true

media_folder: "public/images/cakes"
public_folder: "/images/cakes"

collections:
  - name: "cakes"
    label: "Cake Gallery"
    folder: "src/content/cakes"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Category", name: "category", widget: "select", options: ["Wedding", "Birthday", "Kids", "Holiday", "Trending"] }
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Description", name: "body", widget: "markdown", required: false }
